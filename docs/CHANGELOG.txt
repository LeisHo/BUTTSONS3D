BUTTSONS3D -- CHANGELOG
================================================================================

NOTE (2026-09-04): This file's template was never actually filled in despite
real project history already existing (FBX model integration, OrbitControls
camera rework, the stuck-pressed-button pointer-capture fix, the geometry
outline system, saved camera presets, settings schema versioning, etc. --
all committed to git separately, e.g. commits a0b0fa9 and 0a8cfeb, without a
matching changelog entry). Per CLAUDE.md 4a, that gap is noted here honestly
rather than backfilled with reconstructed entries; this is the first real
entry, not a continuous history from project start.

--------------------------------------------------------------------------------
2026-09-04 -- 03:44 AM (single timestamp; session start not separately measured)

Fixed the screen-space OutlinePass outline toggle (`outlineUsePostprocessing`)
being invisible, and updated the Rhino-exported BUTTON MODEL source files to
the user's latest export.

Fixed:
- OutlinePass composites its edge color with THREE.AdditiveBlending by
  default. This project's outline color defaults to black (#000000), and
  additive blending with black adds (0,0,0) to the scene -- a no-op. The
  pass was rendering every frame, just invisibly, for exactly the color
  everyone was testing with. Root-caused by live-debugging in the browser:
  setting visibleEdgeColor to cyan made the outline immediately visible,
  confirming the pass itself worked and the color was the issue. Fix: set
  `pass.overlayMaterial.blending = THREE.NormalBlending` on both
  `outlinePassBase` and `outlinePassCap` (index.html) so the edge alpha-
  blends over the scene instead of adding to it, which composites correctly
  for any edge color including black.

Updated:
- `data/BUTTON MODEL.fbx` (+ `.fbxbak`) replaced with the user's latest
  Rhino export. Verified via live FBXLoader inspection in the browser: all
  4 named parts (BUTTON_BASE, BUTTON_EXTERIOR, BUTTON_INTERIOR_-_UNPRESSED,
  BUTTON_INTERIOR_-_PRESSED) are present with bounding boxes identical to
  the prior version, so no code changes were needed. Also answered a
  standing question from the user: Rhino's FBX export does not preserve the
  Active/Archived master-layer structure from the source .3dm -- only
  flat object-level names survive in the export -- so duplicate "Active"
  sublayer names under different master layers are not a source of
  ambiguity for this pipeline, and Archived-layer geometry is confirmed
  absent from the exported file.

Verified:
- Live in the Browser pane, both before and after the fix: toggled the
  "Outline: Use Screen-Space Pass" checkbox on a clean page reload and
  screenshotted the result. Before the fix, the outline was essentially
  invisible against the grid; after, it renders as a bold black silhouette
  matching the existing geometry (inverted-hull) outline's appearance.
  Confirmed the checkbox's off state (geometry outline) is unaffected.
  Checked the browser console for errors after reload -- none. Timing was
  not captured via start/finish shell timestamps this turn (task picked up
  mid-session after a context-compaction resume) -- logged as
  Actual: NOT MEASURED in ETA History accordingly.

--------------------------------------------------------------------------------
2026-09-04 -- 03:55 AM - 03:59 AM EDT

Added 3 Debug-group checkboxes to independently hide the base, exterior,
and interior model parts, per direct request.

Added:
- "Hide Base Model", "Hide Button Exterior Model", "Hide Button Interior
  Model" checkboxes, each toggling `mesh.visible` for the corresponding
  per-cell mesh(es). The interior meshes (unpressed/pressed) already swap
  visibility every press/release independent of this new flag, so that
  logic was consolidated into one `updateInteriorVisibility(cell)` helper
  (called from `pressCell`/`releaseCell`/`cancelPressCell`/
  `regenerateGrid`) instead of being set inline in 4 places -- otherwise
  the debug hide flag would get silently overwritten the next time that
  cell was clicked.

Verified:
- Toggled each checkbox individually in the Browser pane on a clean
  reload and screenshotted the result: hiding the exterior leaves the
  interior visible on its own; hiding the interior leaves a hollow
  exterior shell; hiding the base removes the red cylinder under each
  button, exposing the ground. Clicked a button with the interior hidden
  -- no console errors, press/release still functions. Reverted all 3 to
  off and confirmed no errors remained.

--------------------------------------------------------------------------------
2026-09-04 -- 04:17 AM - 04:42 AM EDT

3-item round: user asked to port OKCILCOKCID's camera architecture, then 2
more reports arrived mid-turn while that was in progress ("click and drag
to reorder function doesnt work well. the dragged element becoms dark",
"outlien doesnt work well. Its making an outline for ALL BUTTONS and ALL
BASES etc... AND the outline doesnt work in black. looks like a blending
issue").

Added:
- Camera architecture ported from OKCILCOKCID: position sliders (X/Y/Z)
  now translate camera.position AND controls.target together (true
  world-space translation, doesn't swing the view angle -- the same
  "you provided me rotation sliders only" bug that project's own history
  already fixed once). Added Camera Yaw/Pitch sliders (rotate target
  around a fixed position/distance -- a genuine "turn in place",
  independent of position/zoom) and a Camera Zoom slider (distance-to-
  target). Camera panel values now sync from live state every frame
  (previously only on Copy/Save Settings clicks), so mouse-driven
  orbit/pan/zoom shows up in the sliders immediately. Camera presets now
  store position+FOV+Zoom only (no target), matching the reference
  project's exclusion of target from a preset's identity. Removed
  cameraTargetX/Y/Z; bumped SETTINGS_SCHEMA_VERSION to 3.

Fixed:
- Drag-to-reorder rows/groups reading as "becoming dark" while dragging:
  root cause was `opacity:0.4` on the dragged item, which against this
  panel's near-black background made the text nearly disappear instead
  of clearly indicating what's being moved. Swapped for full opacity
  plus a light background tint and an accent-colored outline.
- OutlinePass (screen-space outline) merging neighboring buttons into
  one connected outline: it draws an edge only where its own selection's
  mask touches an UNselected pixel, so it can't tell two different
  SELECTED objects apart -- with one shared selectedObjects array per
  mesh-type across the whole grid, any two orthogonally-adjacent
  same-type cells whose silhouettes touched on screen merged into one
  outline (visible as long wavy lines running down entire rows). Split
  each mesh-type into a checkerboard 2-coloring by grid cell
  ((row+col)%2) -- 4 OutlinePass instances instead of 2 -- so no two
  orthogonally-adjacent cells ever share a selection. Not a full
  per-object fix (that needs one pass per cell, infeasible for a grid
  this size); a diagonal touch between same-color cells could still
  merge, a rarer case than the row-wise merging this specifically fixes.
- Caught and fixed a self-introduced bug while porting the camera
  architecture, via direct live camera/target inspection rather than
  visual inspection alone: an initial draft kept a redundant wholesale
  position re-apply after the per-slider replay loop in
  applyAllSettings(), which silently clobbered the Camera Zoom slider's
  own already-correct replay on Reset/boot. Removed the now-dead
  applyCameraSettings() wholesale function entirely.

Verified:
- Camera: live-tested position-translate (Yaw/Pitch/Zoom stayed exactly
  constant while translating), Yaw-rotate (position/Zoom stayed exactly
  constant while rotating), Zoom (direction stayed constant), Reset (via
  window.__debug live inspection -- confirmed position/target/zoom end
  up mutually consistent, not just visually plausible), and Save/Use
  Camera preset round-trip (exact value match). Confirmed a fresh reload
  renders identically to before the port (no regression to the default
  view).
- Drag-dark fix: reproduced the exact pre-fix visual (opacity 0.4,
  computed style confirmed) via real PointerEvent dispatch on the grip
  element, then confirmed post-fix computed style (opacity 1, tinted
  background, accent outline) and that the class is correctly removed on
  drop.
- Outline merging fix: screenshotted before (long connected wavy lines
  across rows) and after (each button visibly separated) with a real
  reload between; confirmed a real click still functions with the
  screen-space pass enabled; checked the console after each change (only
  pre-existing transient local-dev-server ERR_CONNECTION_RESET entries
  from rapid reloads, no new script errors).

--------------------------------------------------------------------------------
2026-09-04 -- 05:03 AM - 05:10 AM EDT

Fixed OutlinePass drawing occluded outlines through foreground buttons,
reported directly with a screenshot: "outlines seem to render fully on
top of everything... The outline of a button being obscured by another
button in front, should not have its entire outline shown. only for
areas where the button is visible."

Fixed:
- OutlinePass's own edgeDetectionMaterial gives a "hidden" (occluded)
  edge pixel the same alpha as a genuinely visible one -- they only
  differ by color (hiddenEdgeColor vs visibleEdgeColor). Under the
  AdditiveBlending this pass used before an earlier fix, that was
  harmless as long as hiddenEdgeColor was black (adding zero draws
  nothing) -- which is exactly why a black VISIBLE edge was ALSO
  invisible, the bug the earlier NormalBlending fix addressed. But
  NormalBlending has no such free cancellation, so occluded edges
  started drawing just as opaquely as visible ones. Patched
  edgeDetectionMaterial's fragment shader (inspected live, not guessed)
  so a hidden edge's alpha is forced to 0 -- contributes nothing under
  any blending mode, true depth-respecting occlusion. Applied via
  runtime string-replace against the exact pinned three@0.169.0 shader
  source, with a console.error guard if the match ever fails on a
  future version bump.

Verified:
- Live in the Browser pane: screenshotted before/after with the
  screen-space outline enabled -- before, outlines traced full circles
  around every button top and cut through occluding neighbors in
  criss-cross patterns; after, outlines only trace the actual visible
  silhouette, correctly stopping where another button occludes. A real
  click still functions with the pass enabled. Checked console on 2
  genuinely fresh tabs (not the long-lived test tab, whose console
  history persists across reloads and showed a stale shader-patch-
  failure error from an earlier whitespace-mismatch attempt) -- zero
  errors on either fresh tab.

--------------------------------------------------------------------------------
2026-09-04 -- 07:35 AM - (in progress) EDT

User reported the OutlinePass occlusion bug "still the same" after a
confirmed hard refresh, contradicting the prior round's own live
verification.

Investigated (no code change from this step):
- Confirmed via `git log origin/main..HEAD` the fix was genuinely
  unpushed, and confirmed this project has no Vercel deployment (no
  .vercel/vercel.json) -- ruled out a stale-deployment explanation.
- Confirmed only one process is bound to port 8938 (no conflicting/
  duplicate dev server instance).
- Re-verified the fix at multiple zoom levels including one matching
  the density of the user's screenshot, using the project's actual
  current grid settings (26 cols x 15 rows) -- could not reproduce the
  reported bug.

Hardened (real change, even without reproducing the bug):
- The occlusion fix's implementation was a targeted find-and-replace
  against the exact stock OutlinePass shader text -- fragile by
  construction (needed 2 rounds of exact-whitespace debugging to even
  match in this session's own test browser). Replaced it with a full,
  self-authored fragment shader (uniform names confirmed against
  OutlinePass.js fetched directly from the CDN), removing the
  dependency on any exact substring surviving intact in a different
  environment. Functionally identical logic; verified working again on
  a fresh tab at the same zoom/settings.

Still unresolved: could not reproduce the user's report even after
hardening. Asked the user for further diagnostic detail (browser
console output) since local re-testing has been exhausted without a
reproduction.

--------------------------------------------------------------------------------
2026-09-04 -- 07:49 AM - 07:56 AM EDT

User pasted a Copy Settings dump ("set defaults") plus 3 requests: an
OutlinePass-specific thickness slider, "weird outline overlap still
occurs with black outlines," and the key diagnostic clue -- "when i
change the outline color, the outline does change, but the black lines
overlapping surfaces dont."

Added:
- "Outline Thickness (Screen-Space Pass)" slider (outlinePassThickness,
  0-4px, maps to OutlinePass.edgeThickness) -- separate from the
  existing Outline Thickness slider, which only puffs the geometry
  (inverted-hull) outline meshes outward in local space and has no
  meaning for the screen-space pass. Relabeled the existing one
  "(Geometry)" to disambiguate.
- Set defaults from the pasted dump (camera position/Yaw/Pitch/Zoom/FOV,
  outline thickness/color/outlineUsePostprocessing now ON by default,
  1 saved camera preset, panel geometry/collapsed-group state) -- no
  git-tracked settings log exists yet for this project, so the pasted
  values became the new defaults directly per CLAUDE.md 12m.

Diagnosed (the important finding):
- Reproduced the user's exact scene (their pasted settings, including
  the narrow 10-degree FOV and far zoom) and found the "black lines
  overlapping surfaces" the user described -- dark seam marks at the
  base-to-cap gap between neighboring buttons. Tested decisively by
  turning outlineEnabled OFF entirely: the dark seams were STILL
  present, proving they are NOT an outline-system artifact at all --
  they're the extension cylinder's own toon-shaded shadow side
  (toonShadowFloor is 0% in the user's settings, i.e. true black
  shadows), made visible in the gaps at this specific steep/narrow-FOV
  camera angle. This directly explains the user's own diagnostic clue:
  changing outlineColor recolors the actual outline (pink, correctly)
  but has no effect on these dark seams because they were never outline
  pixels to begin with.
- Reported this finding to the user with the controlled-experiment
  screenshot (outline off, dark seams still there) rather than
  continuing to treat it as an outline bug.

Verified:
- New thickness slider tested at both extremes (0 and 4) -- visibly
  changes screen-space outline glow/thickness, no console errors.
- Set-defaults values verified against a cleared localStorage (so stale
  saved state from earlier in-session testing couldn't mask whether the
  new DEFAULT_SETTINGS values actually took effect).

--------------------------------------------------------------------------------
2026-09-04 -- 08:02 AM - 08:08 AM EDT

User asked (1) to make sure the geometry outline doesn't waste render
time when the OutlinePass checkbox is on, and (2) to look at the HANDO
project's own outline implementation and add matching sliders.

Investigated (no code change needed):
- Read HANDO's src/main.js outline system in full. Confirmed HANDO uses
  the identical "keep both techniques built, toggle visible/enabled"
  architecture this project already uses -- and that HANDO's own render
  loop always calls composer.render() unconditionally (even in hull
  mode), relying solely on outlinePass.enabled to skip the pass's
  internal work. This project's animate() already goes further: it only
  calls composer.render() at all when outlineUsePostprocessing is on,
  skipping the entire OutlinePass pipeline (all 4 passes' render
  targets) otherwise, and refreshOutlines() already sets the geometry-
  hull meshes invisible (no draw calls) whenever the pass is active.
  Confirmed this project's current performance handling already meets
  (and exceeds) HANDO's own reference approach -- no change made.

Added:
- outlinePassStrength / outlinePassGlow sliders ("Outline Edge Strength
  (Screen-Space Pass)" 0-15, "Outline Edge Glow (Screen-Space Pass)"
  0-5), ported from HANDO's passStrength/passGlow sliders -- this
  project had edgeStrength/edgeGlow hardcoded as constants. Defaults
  kept at the existing hardcoded values (5/0), not HANDO's own (8/0),
  so turning them into sliders doesn't silently change the look.

Verified:
- Both new sliders tested at their extremes (Glow to 5: visible bloom:
  Strength to 15: outlines saturate toward white) -- live in the
  Browser pane, no new console errors (only the pre-existing transient
  local-dev-server ERR_CONNECTION_RESET from rapid reloads).

--------------------------------------------------------------------------------
2026-09-04 -- 08:21 AM - (in progress) EDT

User reported "the entire thing is very very laggy" and pasted a "set
defaults" Copy Settings dump.

Added:
- Set defaults from the pasted dump (smaller 14x14 grid, camera
  position/Yaw/Pitch/Zoom, outline thickness/color/pass strength, a 2nd
  saved camera preset, panel item order/collapsed-group state) per
  CLAUDE.md 12m.

Fixed (the important one):
- Root-caused the lag via direct measurement, not guessing: timed
  composer.render() with gl.finish() synchronization (forces the
  measurement to include actual GPU completion, not just command
  submission) and isolated each OutlinePass instance's own cost by
  toggling .enabled. Found each pass costs a near-constant ~40ms/frame
  in this dev environment REGARDLESS of selected-object count (0/1/2/4
  enabled passes measured 4/43/83/124ms) -- OutlinePass's own internal
  machinery (mask/depth render, edge detection, 2-stage blur, overlay
  composite) is fixed per-pass overhead. This confirmed the 4-pass
  checkerboard split added 2 rounds ago (to fix cross-button outline
  merging) was the direct cause, scaling almost exactly linearly with
  pass count.
- Merged the 4 passes (base-checkerboard-A/B + cap-checkerboard-A/B)
  into 2 (checkerboard-A/B, each cell's base+cap meshes sharing one
  array) -- still fixes the actual reported cross-button merging bug
  (orthogonally-adjacent cells never share a selection); gives up only
  the narrower guarantee that a cell's own base-to-cap seam always
  renders (both now share one pass) -- a much smaller trade than the
  lag it removes.

Verified:
- Prototyped the 2-pass merge live (via window.__debug) before writing
  it into source, confirming the measured win (123.7ms -> 60.2ms) and
  that cross-button merging stayed fixed, before committing to the
  code change.
- After the code change: composer.render() measured at 23.7ms/frame
  (~5.2x faster than the original 123.7ms, a bigger win than the ~2x
  pass-count ratio alone predicts). Re-confirmed cross-button merging
  is still fixed via screenshot (clean per-button outlines, no
  connected wavy lines) and the console stays clean on 2 fresh tabs.

--------------------------------------------------------------------------------
2026-09-04 -- 08:44 AM - 08:52 AM EDT

User: "button should have its own outline and so should the base" --
the previous round's 2-pass lag fix merged a cell's base and cap into
one shared selection per checkerboard color, which fixed cross-button
merging but gave up the button's own base-to-cap seam guarantee.

Fixed:
- Kept the pass count at 2 (no lag regression) but changed the
  object-to-pass assignment: a cell's base and its own cap now always
  land in opposite passes, flipping which pass is which by checkerboard
  color. This guarantees BOTH: a cell's own base-to-cap seam always
  renders, AND two orthogonally-adjacent cells' same-type meshes stay
  in different passes (cross-button merging stays fixed). The one
  remaining gap: a cell's base and its neighbor's cap can share a pass
  -- accepted as rarer than same-type neighbor touches at normal
  spacing, same spirit as the already-accepted diagonal-touch case.

Verified:
- Close-up screenshot confirms a clear seam between the button (cap)
  and base; wider-zoom screenshot confirms cross-button separation
  still holds.
- Re-measured composer.render() and got two inflated readings (109ms,
  203ms) along the way -- diagnosed as a measurement artifact from too
  many concurrent browser tabs holding WebGL contexts (the harness
  auto-opens a file:// preview tab on every edit, and this session had
  accumulated 9 open tabs), not a real regression -- confirmed by
  closing the stale tabs and re-measuring clean at 18.3ms, matching the
  prior round's ~20ms result.

--------------------------------------------------------------------------------
2026-09-04 -- 09:15 AM - 09:48 AM EDT

User reported 3 issues in one message: (1) "the extension cylinder
should reach all the way to the base bottom, i should not be able to
see under the button," (2) "base button outline looks ok. but buttn
outline doesnt seem to be there," (3) "wen i rotate and the outliens
overlap, theres aw eird clipping blending thin."

Fixed:
- Root-caused #2 via direct live investigation, not guessing: isolated
  a single fully unoccluded button (1x1 grid), forced the OutlinePass
  depth-test result to a constant "always visible," and watched the
  cap's own top-rim outline reappear -- confirming the prior round's
  occlusion-hiding fix was misclassifying a plainly visible silhouette
  edge (against empty background, no real occluder) as "hidden,"
  silently suppressing real, wanted outline. Removed the hidden/visible
  distinction entirely from edgeDetectionMaterial -- every detected edge
  now draws at full strength unconditionally.
- #3 not independently root-caused, but the same depth-test
  unreliability is a strong candidate (a test already wrong at rest
  would be expected to flicker with viewing angle) -- fixed by the same
  change; rotation-tested through several angles afterward with no
  artifact observed.
- Investigated #1 by walking the actual BUTTON_EXTERIOR mesh geometry
  (not assumed): confirmed the gap-fill extension's radius already
  exactly matched the exterior's own measured bottom-rim radius (both
  1.5 units). The gap was real anyway -- the exterior's underside curves
  inward above the rim rather than being flat, so a shallow/steep camera
  angle grazes that curve before reaching the true bottom-rim radius,
  making the cap's apparent silhouette narrower than its measured radius
  at exactly that angle. Added a new "Extension Radius Margin" slider
  (extensionRadiusMargin, default 1.5x) as a deliberate overhang on top
  of the already-correct match -- empirically tested 1.3x (still a
  visible sliver) and 1.6x (fully closed at a deliberately steep test
  angle), landed on 1.5x as the default, cross-checked at buttonHeight=1
  (unsquashed) to confirm it doesn't visibly overshoot there either.

Verified:
- All 3 fixes re-tested together on a genuinely fresh tab/reload at the
  project's actual current default settings -- clean console, gap
  closed, per-button outline present, multiple rotation angles tested
  with no clipping artifact, geometry (non-postprocessing) outline mode
  also re-checked (unaffected by the shader change, confirmed the wider
  extension radius doesn't regress it), and a real click still functions
  with no console errors.

--------------------------------------------------------------------------------
2026-09-04 -- 08:43 PM - 08:58 PM EDT

User reported the outline bugs from the previous round were still
happening on their actual phone (deployed Vercel site,
buttsons-3-d.vercel.app), even though desktop testing all session had
shown them fixed: "I can see the base's outlines through the button
object and the button objects themselves don't seem to have outlines."
Also flagged a separate "zoomed super out on startup" issue on desktop,
which they deferred for later.

Fixed:
- Discovered (via `AskUserQuestion` ruling out a stale local-save theory
  for the zoom issue, then fetching the deployed site's actual HTML and
  reproducing live at a mobile viewport) that the outline bug was real
  and reproducible on a completely clean load -- just only at
  devicePixelRatio 2+ (real phones), not the devicePixelRatio 1 desktop
  environment used for all of today's earlier testing.
- Root cause confirmed directly against EffectComposer.js's own source:
  its constructor caches renderer.getPixelRatio() once, and every pass's
  setSize() reuses that cached value forever -- never re-reading the
  renderer's live pixel ratio. This project constructs the composer
  before resize() ever runs (resize() is what raises the renderer's
  pixel ratio to match the device, up to 2x); on desktop that cached "1"
  happens to already be correct, which is exactly why extensive desktop
  testing never caught this. On a real high-DPI phone it left the 2
  OutlinePass instances' own internal render targets sized for 1x while
  the main canvas actually draws at 2x -- a genuine resolution mismatch.
- Fixed by calling composer.setPixelRatio(renderer.getPixelRatio()) in
  resize() -- a real method the class already exposes.

Verified:
- Reproduced the exact bug (funnel-shaped gaps, no button outline) at a
  devicePixelRatio:2 mobile viewport on a clean load, both against the
  live deployed site directly and locally via resize_window mobile
  emulation, before writing any fix.
- After the fix: clean outlines at devicePixelRatio:2, confirmed desktop
  (devicePixelRatio:1) stays visually unchanged (correctly a no-op
  there), and a real click still functions with no console errors.

Still open: the "zoomed super out on startup" report on desktop --
deferred by the user for a later round, not investigated this round.

--------------------------------------------------------------------------------
2026-09-04 -- 09:26 PM - 09:46 PM EDT

User sent a screenshot of the deferred "zoomed super out on startup"
report: a tiny diamond-shaped ground plane sliver near screen center, no
buttons visible.

Investigated a wrong lead first, caught it, and corrected course:
- Reproduced a "content confined to one corner" pattern at a custom
  1920x1080 test viewport in this session's own Browser pane tooling,
  and initially treated it as the same bug -- but on closer comparison
  it didn't match the user's actual screenshot (a small diamond near
  CENTER, not content pinned to a corner). Confirmed by retesting at
  the pane's own native viewport size (no custom emulation) that the
  deployed site actually renders correctly -- the corner-confinement
  was a testing-tool artifact specific to a viewport larger than the
  pane's native display area, not a real app bug. Recorded this
  explicitly rather than shipping a fix for the wrong problem.

Fixed the real issue:
- BUTTON MODEL.fbx is a genuine binary asset fetch, unlike everything
  else on this page. There's a real window between page load and the
  model finishing download+parse where regenerateGrid() no-ops (its own
  modelReady guard) -- during that window the ground plane sat at its
  raw, never-yet-scaled PlaneGeometry(1,1) size (groundSize scaling only
  happens inside regenerateGrid()), rendering as an almost-invisible
  sliver near screen center with no buttons at the default camera
  distance/FOV -- exactly matching the report. No loading indicator
  existed to explain the wait.
- ground.visible now starts false, set true only the first time
  regenerateGrid() actually runs with real geometry/scale. Added a
  simple "Loading model..." text overlay, shown by default and hidden
  once the model load promise resolves (success or failure alike).

Verified:
- Confirmed the deployed site renders correctly at the test tool's
  native viewport size, ruling out the corner-confinement artifact as
  related to this bug.
- Directly simulated the pre-load visual state (overlay shown, ground/
  buttons hidden) -- confirmed it now reads as a clean loading message
  instead of a confusing tiny artifact -- and confirmed the overlay
  correctly auto-hides after a real load completes, no console errors.

--------------------------------------------------------------------------------
2026-09-05 -- 07:13 PM - 07:29 PM EDT

User reported "i see a geoemtry that is a wider diameter than my base.
even if i click hide base, hide button ext, hide button int, its still
visible" and "your outline is still showing on top of models." Also
suggested trying an OutlinePass pass-reordering tip from a Discourse
thread.

Checked the pass-reordering suggestion first: this project's pipeline
already matches it exactly (RenderPass, then both OutlinePass instances,
then OutputPass, no FXAA at all) -- told the user directly it wouldn't
change anything rather than applying it as busywork.

Fixed the real cause (already suspected from code inspection before any
live testing):
- extensionRadiusMargin's 1.5x default (added 2 rounds ago to fix "I
  should not be able to see under the button") was tuned only against
  the button's own curved underside, without checking it against the
  other thing the extension connects to -- the base. At default
  settings, extRadius (2.25) genuinely exceeded NATURAL_BASE_RADIUS
  (2.03125), so the connector visibly poked out past the base's own
  edge. It also isn't covered by any of the 3 existing hide toggles, so
  none of them could hide it.
- Clamped extRadius to Math.min(margin-adjusted radius, the base's own
  current radius) so the extension can never be the widest thing in the
  stack. Added a 4th debug toggle (debugHideExtension) to isolate it on
  its own too.

Verified: reproduced the exact bug live (checked all 3 existing hide
toggles, the wider ring with its own outline stayed fully visible)
before fixing; after the fix, all 4 toggles together correctly empty
the scene, the default view is clean, and the earlier under-button-gap
fix still substantially holds (a much smaller residual sliver in some
gaps vs. the original wide funnel-shaped gap -- an acceptable trade for
no longer overshooting the base). Real click still functions, no
console errors on a fresh tab.

--------------------------------------------------------------------------------
2026-09-05 -- 09:12 PM EDT (single timestamp; round start not separately measured)

User reported, with a screenshot: "the button extension should be the same
diameter as the button exterior, not the base," plus three more outline bugs:
"1. Base Outline should not show ontop of the button. 2. Button has no
outline. 3. what is that white artegact in the blackoutline?" Also relayed a
Discourse suggestion about OutlinePass render-pass ordering.

Fixed the extension-diameter regression:
- The prior round's Math.min() clamp against the base's own radius was
  insufficient -- NATURAL_BASE_RADIUS (2.03125) is itself wider than
  NATURAL_EXTERIOR_RADIUS (1.5), so clamping to the base still left the
  extension visibly wider than the button. Removed the base-relative clamp
  entirely; extRadius is now a direct buttonRadius * NATURAL_EXTERIOR_RADIUS
  * extensionRadiusMargin, and extensionRadiusMargin's default changed from
  1.5 to 1 (exact exterior match, no widening). The slider (1-2.5x) still
  exists for deliberately re-introducing an overhang later.

Fixed the outline bugs (items 1-3) by switching the default outline
technique from the screen-space OutlinePass to the existing geometry
(inverted-hull) technique:
- Root cause: OutlinePass runs as 2 shared passes split by a checkerboard +
  base/cap-swap scheme (added earlier this session to fix lag while keeping
  the base-to-cap seam). That scheme guarantees the seam and orthogonal
  neighbors land in opposite passes, but diagonally-adjacent same-parity
  cells can still share a pass and their edges merge into one connected
  blob on screen -- reading as "no outline on this button" or "the wrong
  part's outline" depending on which edges happened to merge. This was
  already flagged as a known limitation earlier in the session but measured
  as far more visually severe than originally estimated once tested at
  realistic grid density (long connected wavy lines across many buttons).
- Verified via live A/B at identical camera angle, grid density (14x14 and
  a zoomed-in 3x3), and both the default dark and a light test ground
  color: Geometry mode showed zero merging and zero seam/background
  bleed-through in every case; OutlinePass mode showed the diagonal-merge
  artifact in the same shots. Switched DEFAULT_SETTINGS.outlineUsePost-
  processing from true to false. The checkbox remains for switching back
  to OutlinePass, documented in-code as a known-limitation trade-off.
- A true fix for OutlinePass would need a 4-way (row%2, col%2) split,
  doubling the pass count back to 4 and likely reintroducing the lag this
  session already fixed once (4 passes -> 2) -- not pursued since Geometry
  mode already has none of these problems.
- Checked the Discourse pass-reordering suggestion against the actual
  composer.addPass() call order: already matches it exactly (RenderPass,
  both OutlinePass instances, OutputPass, no FXAA at all). Told the user
  directly it would not change anything rather than applying it as
  busywork.
- Also resolved an apparent discrepancy where the debugHideExtension
  checkbox seemed not to hide anything in a live screenshot: verified via a
  temporary debug hook that the checkbox, settings.debugHideExtension, and
  every cell's extMesh.visible were all correctly in sync (false -> true ->
  every extMesh hidden) -- the toggle works correctly; the earlier
  screenshot's unchanged appearance was the visible cap/base geometry
  itself, not the extension mesh. Debug hook removed before commit.

Verified: fresh reload at default settings (14x14 grid, Geometry mode) --
clean outlines on every button and base, no merging, no white/background
artifact, no console errors, real click still functions.

--------------------------------------------------------------------------------
2026-09-05 -- 09:50 PM - 10:04 PM EDT

User reported, with an annotated screenshot of 2 overlapping buttons: (1) the
extension should reach from the base's own bottom up to the button's bottom,
masked beneath the base, not just from the base's top; (2) green arrows --
white artifacts where different parts' outlines meet; (3) blue arrows -- the
base's outline showing on top of the button/extension within the same
instance; (4) pink arrow -- a button's outline should show on top of any
other instance's geometry that's visually behind it; (5) purple arrow -- an
instance's outline that's behind another instance should never show on top
of the front instance's geometry.

Fixed the extension span (item 1):
- updateExtension() previously spanned from the base's TOP (baseTop) to the
  cap's bottom. Changed to span from group-local y=0 (the base's own bottom,
  same origin as baseMesh) to the cap's bottom, so the lower portion is
  simply hidden inside the base's own opaque geometry rather than stopping
  exactly at the base's top edge. Verified live by isolating the extension
  alone (all 3 other debug hide toggles on): now a clean straight-sided
  cylinder from the ground up to the button, not just base-top upward.

Fixed the in-group base-outline-over-button overlap (item 3, blue arrows):
- Root cause: addOutline()'s default puffs all 3 local axes ("xyz"). Since
  baseMesh's local origin sits at its own bottom (group y=0), puffing Y
  scales distances from that origin -- the bottom stays fixed but the TOP
  face moves up by baseHeight*outlineThickness, directly into the cap's own
  space (the cap sits flush on the base's real top). Fixed by passing "xz"
  to addOutline(cell, baseMesh, ...), matching the treatment extMesh's own
  outline already used for exactly this reason.

Fixed the white/light artifact at outline junctions and the cross-instance
depth-ordering complaints (items 2, 4, 5 -- green/pink/purple arrows):
- These traced to a single cause, not 3 separate bugs: outlineThickness's
  default (0.21) puffs the inverted-hull outline shells far enough that they
  self-intersect with the real mesh's own concave/complex features (the
  base's non-flat underside, the step where the narrower cap sits on the
  wider base) -- not a depth-sort or render-order bug, a geometry
  self-intersection at that thickness. Confirmed by toggling outlineEnabled
  off entirely on the exact same close-up 2-button framing: the light
  streaks were not present on the bare solid geometry, proving they were
  outline-shell-generated. Then confirmed thickness dependence directly:
  0.21 reproduced a jagged, lighter-colored crown of artifacts around the
  base's underside and at the cap/base shoulder corner (matching the
  reported green-arrow locations); 0.05 removed it entirely; 0.1 also
  removed it while staying clearly visible as an outline, checked at both a
  close 2-button framing (matching the user's screenshot layout: 1 column, 2
  rows, oriented and zoomed to reproduce the reported overlap) and the full
  default 14x14 grid. Changed DEFAULT_SETTINGS.outlineThickness from 0.21 to
  0.1. No render-order or depth-test bug was ever found for items 4/5 --
  once the self-intersection artifact was gone, both the "outline in front
  of the wrong instance" and "outline behind, showing through" symptoms were
  gone too, since normal opaque z-buffer depth testing already handles
  cross-instance ordering correctly once the geometry itself isn't
  self-intersecting.

Verified: fresh reload at default settings (14x14 grid) -- clean outlines
throughout, no merging, no white/light artifacts, no console errors, real
click still functions. Also re-verified the exact reported scenario (a 1x2
grid rotated/zoomed to reproduce the screenshot's 2-button overlap) after
baking all 3 fixes into DEFAULT_SETTINGS: no blue/green/pink/purple artifacts
at any of the flagged junctions.

--------------------------------------------------------------------------------
2026-09-05 -- 10:25 PM - 10:29 PM EDT

User sent a new annotated screenshot reusing the same 4 arrow colors from the
immediately preceding round ("outline issues are exactly the same") plus one
new one: an orange arrow -- the extension's own outline should not show on
top of either the base or the button.

Fixed the orange-arrow item: removed addOutline(cell, extMesh, "xz")
entirely. Root cause: the extension now spans the base's full height (0 to
capBottom, per the previous round's fix), so its puffed-radius outline shell
now also runs alongside the base's own full height, not just the small
visible gap -- and pokes past the base's real silhouette wherever the base's
profile is narrower than extRadius*(1+outlineThickness) at that height. The
extension is a plain structural gap-filler; the base's top-rim outline and
the cap's bottom outline already visually frame the seam, so the extension
never needed its own separate outline.

Investigated the "exactly the same" claim for the other 4 (blue/green/pink/
purple): re-verified the prior round's fixes from a completely fresh
localhost tab -- same close 2-button framing as before plus the full default
14x14 grid -- and found them still clean, matching the prior round's own
verification exactly. Checked this project's settings-persistence mechanism
(loadSavedDefaults(), LOCAL_KEY "buttsons3dSettings"): Save Settings writes
the CURRENT slider values to localStorage, and loadSavedDefaults() prefers
that saved dump over DEFAULT_SETTINGS for every key it contains (via
Object.assign), unconditionally, as long as its _schemaVersion still matches
SETTINGS_SCHEMA_VERSION (currently 3, unchanged all session). Neither of the
last 2 rounds' default-value changes (outlineThickness, outlineUsePost-
processing, extensionRadiusMargin) bumped that version, since they didn't
change a setting's MEANING (the documented bar for a version bump) -- only
its default value. This means if the user's own browser has an old Save
Settings snapshot from earlier in this session (a real possibility --
CLAUDE.md's own §12m "set defaults" workflow documents this project's
Copy/Save Settings usage pattern), every one of the last 2 rounds' code-level
default fixes would be silently masked on their end without any error,
while a fresh/never-saved browser (this session's own verification tab)
shows them fixed. Did NOT bump SETTINGS_SCHEMA_VERSION preemptively --
that would also discard any legitimately-customized camera position/grid
layout the user saved on purpose, which is a real cost this project's own
documented version-bump policy exists to avoid paying unnecessarily. Flagged
this to the user directly instead of guessing silently, asking them to check
their own saved settings / hard-refresh before assuming the code fix itself
didn't take.

Verified: fresh reload at default settings (14x14 grid) and the exact
reported 2-button overlap scenario -- no orange/blue/green/pink/purple
artifacts, no console errors, real click still functions.

--------------------------------------------------------------------------------
2026-09-05 -- 10:58 PM - 11:17 PM EDT

User corrected the scope of the last 2 rounds' outline work: "im talking
about the outline pass Screenspace pass one, not the negative hull
outlines." The last 2 rounds had fixed the geometry (inverted-hull)
technique, which had since become the default -- but the user's own
screenshots and complaints were about OutlinePass (screen-space), which
this project's dev panel checkbox can still switch to. None of those 2
rounds' fixes touched OutlinePass at all.

Reproduced the reported 2-button overlap scenario specifically with
outlineUsePostprocessing forced on. Found and fixed a real bug: extMesh
(the gap-fill extension) is included in OutlinePass's "cap" pass group,
and since it now spans the base's full height (0 to capBottom, a fix from
2 rounds ago), and OutlinePass draws every pass's edges unconditionally
(the hidden/visible depth check was removed earlier this session after
being found unreliable), the cap-pass's outline drew extMesh's own
silhouette edges straight through the base's visible surface -- a
vertical line cutting through the base, matching the reported "base
outline on top of button" / new orange-arrow-style complaint. Fixed by
removing extMesh from both outlineObjectsA/B arrays. Confirmed live:
toggling debugHideExtension off made the lines disappear with the
extension's solid mesh still correctly hidden inside the base.

Investigated a follow-up regression this fix seemed to reveal: a white/
light artifact exactly at the base-to-cap shoulder corner, matching the
green-arrow complaint from 2 rounds ago -- confirmed real via live A/B
against a lighter ground color. Attempted a targeted fix (a second,
always-selected-but-invisible "outline helper" mesh spanning just the
small visible gap) and reverted it after live-testing proved it did
nothing: OutlinePass ignores objects with visible=false entirely
(confirmed by scaling one to 6x with zero visible effect), and at rest
capGroup already sits exactly at baseTop (capBottom - baseTop = ~0), so
there's no real 3D gap there for a helper to fill regardless. Also ruled
out "edge too thin/weak" as the cause -- outlinePassThickness and
outlinePassStrength both tested at their max values with no visible
change to the notch. Root cause is most likely (not independently
confirmed) that base and cap are 2 separate OutlinePass instances, each
running a fully independent mask/edge-detect/blur/overlay pipeline with
no awareness of the other's geometry -- a sub-pixel gap between 2
separately-rasterized edges at their shared boundary that no blur radius
or strength can close, since a pass can't dilate an edge it never
detected. A real fix would need the seam drawn as one continuous edge by
a single pass, which conflicts with the whole reason 2 passes exist
(giving base and cap separate outlines at all).

Documented the cross-instance depth-ordering complaint (pink/purple
arrows) as the same, already-known, already-accepted 2-pass-budget
ceiling from 2 rounds ago -- confirmed via live testing to be more
visible at a dense grid/close camera than originally estimated, but not
attempted this round: fully separating every touching pair (orthogonal +
diagonal neighbors, base + cap) needs more independent pass groups than
the measured ~40ms-per-pass fixed overhead can afford without
reintroducing the "very very laggy" regression from earlier this
session.

Net result this round: 1 real, verified bug fixed (extension outline
through the base). 2 issues (corner white artifact, cross-instance
depth-ordering) investigated in real depth, ruled out several plausible
fixes with live evidence, and documented in-code as known architectural
limitations of the 2-pass OutlinePass technique rather than left
unexplained -- the geometry (inverted-hull) technique has neither
limitation, confirmed via this session's own earlier live A/B testing.

Verified: fresh reload, no console errors, real click functions, the
extension-through-base line is gone in both the reported 2-button overlap
scenario and the full default grid.

--------------------------------------------------------------------------------
2026-09-06 -- 12:03 AM - 12:33 AM EDT

User asked for the OutlinePass pass budget's previous "very very laggy"
threshold, then requested a concrete experiment: "make it three passes and
see how that is" (2 was the working ceiling, 4 was rejected as laggy, 3 was
never actually tried).

Before writing any code, worked out on paper that 3 passes cannot achieve
full separation of every touching pair -- properly coloring a grid so no
two orthogonally- or diagonally-adjacent cells share a color needs 4
colors minimum (basic graph coloring, not a design choice). Told the user
this upfront so the experiment's expectations were set correctly.

Implemented a 3-way split: cellOutlineColor(r,c) merges the 4 (row%2,
col%2) parity classes down to 3 slots (the 2 "same-parity" classes --
both-even, both-odd -- combined into one), with each cell's cap using
(cellColor+1)%3 so base and cap always land in different passes (same
guarantee the old 2-pass swap gave, generalized). Added a 3rd OutlinePass
instance (outlinePassC) and updated the pass-assignment/selection code
accordingly.

Measured real frame cost with the same gl.finish()-synchronized technique
documented earlier this session, 3 separate runs of 60 frames each, default
14x14 grid: 0/1/2/3-pass totals of 7.65/16.4/22.1/29.8ms,
8.8/21.4/30.0/46.4ms, and 4.9/20.2/29.1/38.0ms. Full 3-pass cost measured
29.8-46.4ms across runs -- noisier AND substantially lower than the
~40ms/pass, ~83ms-at-2-passes figures documented earlier this session from
the same technique. Reported both real numbers honestly rather than
picking one; the discrepancy's cause (GPU/driver/thermal state, session
history, etc.) was not independently investigated -- the actual point of
this experiment was the 3-vs-2 relative comparison, and at this newly-
measured scale both are far under the old rejected 4-pass figure (124ms).

Verified the diagonal-merge improvement live: checked cellOutlineColor()'s
prediction by hand (orthogonal same-type neighbors always separated,
confirmed for all 8 parity transitions; diagonal same-type neighbors fixed
for exactly half of all cells, per the unavoidable 3-vs-4-colors ceiling)
against a 4x4 dense grid screenshot -- most buttons now show fully
separate outlines, with occasional pairs still merging (the predicted,
mathematically-unavoidable same-parity case) rather than the long
connected wavy chains reported for the 2-pass version. Also re-checked the
exact 2-button overlap scenario from 2 rounds ago (pink/purple arrows):
clean in this specific configuration, though the theoretical base-vs-
neighbor's-cap collision analysis (4 of 8 orthogonal transitions still
collide, no better or worse than the 2-pass version -- this change wasn't
targeted at that case) means it can still surface elsewhere. Documented
all of this directly in the pass-construction and pass-assignment comments.

Verified: fresh reload (a genuinely new tab, not just a reload, after a
stray net::ERR_CONNECTION_RESET from an abandoned rAF-based FPS experiment
turned out to be a stale console entry, not a real fault) -- no console
errors, real click functions, default Geometry-mode outline unaffected
(this change only touches the OutlinePass code path).

--------------------------------------------------------------------------------
2026-09-06 -- 12:44 AM - 01:10 AM EDT

User exported a new model, `data/BUTTON MODEL WITH OUTLINE.fbx`, and asked to
switch to it. Clarified mid-turn: "that fbx is the same model but with a
outline emission material applied to create an outline."

Investigated the new file's structure before touching any code (loaded it
live through this project's own FBXLoader import, traversed the hierarchy):
all 4 parts come through as generically-named meshes ("Object_1/3/4/5", not
"BUTTON_BASE" etc. -- the descriptive names were lost in this export), each
with `geometry.groups` splitting its faces across 2 materials -- the normal
"Material_2" plus an outline-named one (verbatim from the file, typos
included: "OUTLINE", "BUTTONOULINE", "OUTLINE INT", "PRESSOUTLINe"). This
matched the user's description exactly: each part now carries its own
baked outline shell as a 2nd material group on the same mesh.

Since names are gone, rewrote part identification to use bounding-box
radius instead, matched against this project's own existing
NATURAL_BASE_RADIUS/NATURAL_EXTERIOR_RADIUS constants (confirmed close:
~2.05 vs 2.03125, ~1.519 vs 1.5), with the 2 remaining interior domes told
apart by height (taller = unpressed, shorter = pressed, consistent with
pressing pushing the dome down). Caught and fixed one real bug during this:
the first version measured bounding boxes on the RAW (Z-up, unrotated) FBX
mesh instead of the already-rotated Y-up geometry, silently reading radius
and height on the wrong axes and producing a completely broken render.

Attempted to actually render the baked outline shell (multi-material mesh,
group 0 = color, group 1 = a new bakedOutlineMat) before falling back:
tried BackSide culling (same technique this project's own existing
procedural outline already uses successfully) -- the base rendered
correctly as a thin rim, but the exterior/cap came out solid black almost
everywhere. Isolated further with dedicated test materials per group: even
within the exterior alone, its cylindrical side wall culled correctly while
its flat top face did not. Confirmed via a live wireframe overlay that the
outline shell IS a genuine small enlarged duplicate (not a modeling/export
error) -- the problem is inconsistent winding/offset convention across
parts (and even across faces within one part), not a single flippable
setting. Concluded a single side value can't fix all 4 parts correctly
without per-part (or per-face) special-casing, which wasn't pursued this
round.

Fell back to a clean, working alternative: extractGeo() now keeps only
material group 0 (the real surface) and discards group 1 entirely, so this
new file's shape gets used as requested without shipping a visibly broken
baked outline. The existing, already-reliable procedural outline systems
(the geometry inverted-hull technique, and the OutlinePass screen-space
toggle) remain the actual source of the visible outline, unaffected.

Also committed `data/BUTTON OUTLINES.blend`, the Blender source file the
user dropped alongside the FBX export (referenced inside the FBX's own
embedded path strings) -- consistent with this project's existing
convention of tracking source files (.3dm/.3dmbak) alongside their
exports. Left the old `BUTTON MODEL.fbx`/`.fbxbak` in place, unreferenced
but not deleted, per this project's standing convention.

Verified: fresh reload (a genuinely new tab) shows the full default grid
correctly colored with clean geometry-mode outlines, no console errors,
real click/press interaction still functions.

--------------------------------------------------------------------------------
2026-09-06 -- 01:26 AM - 01:44 AM EDT

Follow-up to the FBX-era baked-outline investigation (previous entry). User
asked directly why they still couldn't see their offset emission outline
(answer: it was deliberately excluded from rendering, per that entry), then
exported the SAME model as glTF instead and asked to try that, and
separately asked whether exporting each part as its own file would help.

Switched the loader from FBXLoader to GLTFLoader, pointed at the new
data/BUTTON OUTLINES.glb. Structural difference from the FBX worth noting:
glTF split each part into 2 separate MESH objects (e.g. "Object_5_1"/
"Object_5_2") instead of 1 mesh with 2 material groups -- easier to reason
about, split here by matching each mesh's own material name against
"Material_2" (the real surface) rather than relying on the "_1"/"_2"
naming suffix. Also found and fixed a fresh bug from this format switch:
glTF is Y-up by spec (Blender's own exporter handles the Z-up-to-Y-up
conversion automatically), so the FBX-era rotateX(-90deg) call left over
in extractGeo() was rotating an already-correctly-oriented mesh an extra
90 degrees, producing a bogus bounding box. Removed it for the glTF path.

Re-tested the baked outline shell rendering with the exact same method as
the FBX round (child mesh per part, BackSide black material, procedural
outline disabled to isolate it): identical failure, same parts affected
(base fine, exterior/cap solid black). This is the important negative
result -- it rules out "FBX export quirk" as the cause, since 2 unrelated
export pipelines (FBX and glTF) both faithfully reproduced the same broken
winding. The problem lives in the outline shell's own mesh data as
authored in Blender (inconsistent/unrecalculated normals), not in how it
gets exported -- confirmed this conclusion before answering the "separate
files" question too: loaded one of the user's new per-part files
(BUTTONEXTERIOR-OUTLINES.glb) and directly compared its vertex count and
sampled vertex radii against the combined file's own exterior mesh --
identical (305 vertices, same ~1.48/1.52 radius pattern both times),
confirming it's the exact same underlying geometry data just repackaged,
so splitting into separate files wouldn't have changed anything either.
Answered that question directly rather than leaving it untested.

Net result unchanged from the FBX round: the new model's shape/colors are
in use (now via glTF), the baked outline shell is extracted and identified
but still not rendered, and this project's own existing outline systems
remain the actual visible outline. Committed the 4 separate per-part glb
files the user exported alongside the combined one, even though unused by
code currently, consistent with this project's practice of tracking
source/export assets rather than leaving them as untracked working-tree
files.

Verified: fresh reload (new tab) shows the full default grid rendering
correctly, no console errors, real click still functions.

--------------------------------------------------------------------------------
2026-09-06 -- 02:10 AM - 02:25 AM EDT

User recalculated normals on the outline shell in Blender (per the previous
round's specific suggestion) and re-exported BUTTON OUTLINES.glb, asking to
try again.

THE BAKED OUTLINE NOW WORKS. Confirmed the new file was actually being
loaded (not a stale cache -- checked the response's Last-Modified header
and byte count directly against the file on disk) before concluding
anything. Re-tested with the same BackSide setting that had partially
worked in the very first FBX attempt: this time it's wrong almost
everywhere -- the recalculation evidently flipped the shell's winding
uniformly (from inconsistent-per-face to consistent-but-reversed).
Switched to FrontSide and it rendered correctly across all 4 parts on the
first try. Verified thoroughly before committing to the change: full
default 14x14 grid, both the default dark and a lighter ground color (no
white artifacts anywhere), a real click, and (separately) that it doesn't
visually conflict with either the existing Geometry-mode procedural
outline (both active together still looks like one clean outline, no
z-fighting) or the OutlinePass screen-space toggle (co-exists fine,
OutlinePass's own known diagonal-merge limitation is unaffected either
way).

Wired it in for real this time: bakedOutlineMat (FrontSide, following the
shared outlineColor setting) added as a child of each of the 4 real parts,
using the authored outlineGeo shells extracted in the previous 2 rounds'
work (no changes needed there -- extraction/identification was already
correct, only the rendering side was ever broken). Made it always-visible
regardless of the outlineEnabled toggle, since it's now authored into the
model itself rather than being one of this project's own outline
techniques -- same treatment as the base/button colors being part of the
model's own look, not a toggleable effect layered on top.

This closes out a genuine multi-round arc: FBX with inconsistent per-face
winding (broken) -> glTF re-export of the same unedited mesh (broken the
same way, ruling out export format as the cause) -> glTF re-export after
Blender-side normals recalculation (works, with the culling side flipped
from what partially worked before). Rewrote the in-code history comments
to tell this whole story in one place rather than leaving 2 separate
"still broken" comments from the earlier rounds standing uncorrected.

Verified: fresh tab, full default grid, no console errors, real click
functions, both outline techniques (Geometry, OutlinePass) still coexist
correctly alongside the now-working baked outline.

--------------------------------------------------------------------------------
2026-09-06 -- 02:38 AM - 02:48 AM EDT

Follow-up to the previous round (baked outline finally rendering
correctly). User asked whether adjusting its thickness required going
back into Blender -- confirmed yes, since it's real offset geometry, not
a procedural effect -- then asked to add a runtime alternative instead of
requiring a re-export every time.

Added bakedOutlineThickness (0 default, range 0-1, "Outline Thickness
(Baked Shell)" in the dev panel's Shading group). Applies a uniform
1+thickness scale to each of the 4 baked outline child meshes -- same
"puff the shell" concept as the existing Geometry-technique's
outlineThickness slider, but a fully independent control, since the baked
shell already covers each part's own true silhouette exactly and needs no
axis-specific exception the way outlineAxisScale()'s "xz"-only case does
for the base/extension.

Added a `bakedOutlineMeshes` array on each cell (parallel to the existing
`outlineMeshes` array for the procedural technique) so refreshOutlines()
can reach and scale these 4 meshes per cell without re-walking the scene
graph.

Verified live: default (0) matches the previous round's committed look
exactly; a single isolated button pushed to max (1.0) shows a dramatically
enlarged shell, confirming the mechanism actually works; a moderate value
(0.1) shows a clean, proportionally thicker outline with no artifacts.
Hit one unrelated, transient issue mid-round -- a "Failed to fetch" error
loading BUTTON OUTLINES.glb that resolved on a plain retry (curl directly
against the dev server confirmed it was answering fine throughout, so this
was a browser-pane-side networking hiccup, not a real regression) -- noted
here rather than silently ignored, since it could otherwise look alarming
in isolation.

Also noticed BUTTON OUTLINES.glb itself changed on disk partway through
this round (same byte count, different hash, mtime matching right when
this round started) -- the user re-exported/re-saved it again in
parallel, outside this conversation. All of this round's own testing
happened after that timestamp, so it was already verified against the
current file; included the updated binary in this commit as-is rather
than questioning what changed inside it.

Verified: fresh tab, default settings, no console errors, real click
functions.

--------------------------------------------------------------------------------
2026-09-06 -- 03:19 AM - 04:02 AM EDT

4-item round, growing mid-turn: (1) user disliked the runtime baked-outline
thickness slider's look and re-exported the glTF with the thickness baked
in directly, asking to just use that; (2) the button extension should share
BUTTON_EXTERIOR's own geometry, scaled vertically, instead of a generic
cylinder primitive; (3) a click should register on the interior dome, not
just the exterior shell; (4) a slider to move the ground plane up/down.

(1) Turned out to already be satisfied: bakedOutlineThickness already
defaults to 0 (no extra scaling), and no newer file existed on disk yet at
the time -- confirmed via file timestamps rather than assuming.

(2) extMesh now shares modelGeo.exterior directly (previously a
CylinderGeometry primitive with its own computed radius). Added
NATURAL_EXTERIOR_HEIGHT (measured live off the actual geometry, 2.00085)
so span/NATURAL_EXTERIOR_HEIGHT gives the correct Y-scale; X/Z scale set
directly from buttonRadius*extensionRadiusMargin (extensionRadiusMargin
kept as an optional extra multiplier, default 1 = exact match).

(3) pickCell()'s raycast target list only ever included exteriorMesh.
Tagged unpressedMesh/pressedMesh with the same userData.cell and added
them to the raycast pool -- an invisible interior mesh sitting where the
visible one is just resolves to the same cell, so no need to pick only
the currently-shown one.

(4) Added groundPositionY (default 0, -5 to 5 range) wired directly to
ground.position.y via a new `groundPos` config flag.

Mid-turn, the user re-exported the glTF twice more while testing was in
progress, surfacing 2 real, unrelated problems along the way:

- The 2nd re-export changed structure: material names now carry Blender's
  ".001" collision suffixes (an exact "Material_2" string match broke),
  and only 2 of 4 parts had outline shells this time instead of 4. Fixed
  by matching material names by PREFIX instead of exact string, and by
  rewriting outline-to-part assignment to match each outline shell
  independently against the 4 already-identified main parts by nearest
  (radius, height) rather than assuming all 4 (or a specific subset) are
  present -- handles any combination of missing outline shells correctly,
  not just "all or nothing."

- That same re-export revealed a real, severe visual break: the interior
  dome's own top rendered as a solid black fill at DEFAULT settings, not
  just a thin rim -- visible on every button in the default grid. First
  suspected the same "xyz"-over-puff issue baseMesh already has a fix
  for, and tried the identical "xz"-only restriction -- confirmed live
  this did NOT help at all (identical solid black either way), ruling
  that theory out. With the procedural outline disabled entirely, the
  dome's own main surface rendered perfectly once the user recalculated
  normals in Blender (root-caused and reported back before the fix
  landed) -- so the remaining problem is the inverted-hull TECHNIQUE
  itself, not the mesh data: the interior dome is viewed from inside a
  concave surface (through the exterior's own opening), the opposite of
  what the BackSide-culled-puffed-shell trick assumes (built for convex
  surfaces viewed from outside). Fixed by dropping the procedural outline
  from unpressedMesh/pressedMesh entirely -- the exterior's own outline
  ring, immediately surrounding the opening, still gives the button a
  clear silhouette edge.

Verified: fresh tab, default 14x14 grid renders correctly (clean red
interior domes, no black fill, no console errors), a real click on the
interior dome registers (confirmed via currentGain changing), the
extension's isolated shape matches the exterior's own dome profile, the
ground-height slider visibly moves the ground plane, real click still
functions with no new console errors.

--------------------------------------------------------------------------------
2026-09-06 -- 04:13 AM - 04:26 AM EDT (12m44s)

Gave the button extension (extMesh, the gap-filling connector between base and
cap) the same baked outline as exteriorMesh, per explicit user request ("my
button extensions shuold also have the same outine as the button exterior").

Fixed/Added:
- extMesh already shares modelGeo.exterior's own main geometry (from an
  earlier round this session). Added a child mesh reusing outlineGeo.exterior
  (the same outline-shell geometry already computed for exteriorMesh) with
  bakedOutlineMat, pushed into the same bakedOutlineMeshes array baseMesh's
  outline already uses -- so it picks up bakedOutlineThickness scaling for
  free. As a child of extMesh it inherits extMesh's X/Z scale (radius match)
  and Y scale (live vertical stretch from updateExtension()), so the outline
  shell stretches with the extension automatically, no extra transform code
  needed.
- This reuses the real, depth-tested bakedOutlineMat shell (FrontSide, normal
  z-buffer occlusion) established earlier this session -- not the earlier
  screen-space OutlinePass/procedural technique that had its own no-occlusion
  artifacts (e.g. the earlier line-through-the-base bug); that failure mode
  doesn't apply to the depth-tested shell approach.

Verified: isolated single extension (1x1 grid, base/exterior/interior hidden
via debug checkboxes) shows a clean black outline ring at the extension's rim
matching the exterior's own outline treatment, no fill artifacts or stray
lines at multiple zoom levels. Full assembly (all parts visible) and the
dense default 14x14 grid both render correctly with no seams or artifacts at
the extension/exterior boundary across every button. Fresh tab: no console
errors (only the pre-existing informational warning that this session's glb
has outline shells for only 2 of 4 parts -- expected, since the interior
domes never had or needed one).

--------------------------------------------------------------------------------
2026-09-06 -- 04:34 AM - 04:43 AM EDT (8m45s)

User asked whether the dev panel's Save Settings button saves to Git -- the
panel's own status text at the time said "Local-only for now -- git-tracked
sync not wired up yet for this project," which was accurate: Save only wrote
to that browser's own localStorage. Wired it up properly, per user's "do
that" (explicit follow-up to this exchange).

Found dev_server.py already exposed a working /api/save-settings POST route
(writes to data/processed/dev-panel-settings.json) that the client never
actually called -- likely built in a prior round in anticipation of this, or
copied from the sibling BUTTSONS project's own scripts/active/dev_server.py,
which already fully implements this exact pattern (client + server) and was
used directly as the reference.

Added/Changed (index.html):
- Save Settings now POSTs to /api/save-settings when served over http(s);
  falls back to the previous localStorage-only behavior under file:// (no
  server to receive the write) -- matches CLAUDE.md 12l's documented
  exception exactly.
- Boot-time settings loading (previously synchronous, localStorage-only) is
  now async: loadSavedDefaults() fetches the git-tracked
  data/processed/dev-panel-settings.json when running over http(s), falling
  back to DEFAULT_SETTINGS on a 404/failure (e.g. before the first-ever
  Save). Slotted into the existing loadButtonModel().then() async boot seam
  via Promise.all() rather than restructuring it -- applyAllSettings()
  already refreshes every panel control from the resolved settings, so the
  panel (already built and visible with placeholder defaults) needed no
  rebuild.
- Reset is unaffected -- it already restores from the in-memory
  savedDefaults, which both Save and the boot-time load now keep correct.
- Updated the panel's own status text to describe the real, now-working
  behavior instead of the "not wired up yet" placeholder.
- No GitHub-API/token half of CLAUDE.md 12l was added -- this project has no
  Vercel (or other) deployment, only a local dev server, so only the local
  read/write half applies; the sibling BUTTSONS project's api/save-settings.js
  (Vercel-only) was not needed here.

Verified: fresh tab loads cleanly (a 404 on the settings file is expected
before any Save exists); clicking Save produces a real 200 POST and the file
lands on disk with real settings content; changed Column Count from 14 to 6,
saved, reloaded in the same tab, and the panel/grid both came back showing 6
-- confirmed via a real value change, not just a status message, that the
round trip (Save -> git-tracked file -> reload -> read-back) genuinely
works. Restored Column Count to 14 and re-saved afterward. Committed the
settings log file itself (data/processed/dev-panel-settings.json) alongside
index.html, matching the sibling project's own convention of tracking it.

--------------------------------------------------------------------------------
2026-09-06 -- 07:37 AM - 07:45 AM EDT (8m41s)

User exported a dedicated glTF for the button extension (the base-to-cap
connector) and asked to use it instead of the earlier stand-in: extMesh
sharing modelGeo.exterior's own geometry, scaled vertically. Added
data/BUTTON EXTENSION OUTLINES.glb (main surface + outline shell mesh, same
"Material_2"-prefix convention as the combined BUTTON OUTLINES.glb) and a new
loadExtensionModel() to load it independently, alongside the existing
loadButtonModel() in Init's Promise.all().

Added/Changed (index.html):
- modelGeo/outlineGeo gained an `extension` slot; new NATURAL_EXTENSION_RADIUS/
  NATURAL_EXTENSION_HEIGHT measured live off the loaded geometry's own bounding
  box (same approach NATURAL_EXTERIOR_HEIGHT already used). New extGeo/
  extNaturalRadius/extNaturalHeight vars hold whichever geometry/dimensions are
  actually in use -- the dedicated extension asset normally, or the old
  exterior-borrowing values as a fallback if the new file ever fails to load,
  so a load failure degrades gracefully instead of crashing or breaking the
  scene.
- extMesh now built from extGeo instead of modelGeo.exterior. Radius scale
  (extRadiusScale) is re-derived through extGeo's own natural radius
  (NATURAL_EXTERIOR_RADIUS / extNaturalRadius, times the existing buttonRadius
  * extensionRadiusMargin) so the extension's real-world diameter still
  matches the button's exactly regardless of what radius the dedicated asset
  happens to be authored at -- this reduces to the exact same math as before
  when extGeo is still the exterior fallback. updateExtension()'s vertical
  scale (span / extNaturalHeight) follows the same pattern.
- The extension's own outline shell (outlineGeo.extension) is used when
  present; only falls back to outlineGeo.exterior when extGeo ITSELF is still
  the exterior fallback (matching shapes) -- never mismatches the exterior's
  outline shape onto the real dedicated extension geometry.

Verified: fresh tab load, no console errors, no "outline shell missing"
warning for the extension (the new file included both a main mesh and its
own outline shell, matched by the established naming convention on the first
try). Isolated single-button view (1x1 grid) at 2 zoom levels shows the
extension's radius lining up exactly with the exterior above it -- no step,
gap, or misalignment at the seam -- with its own clean black outline ring.
Full default 14x14 grid also renders correctly with no artifacts across
every button. Reset (not a settings change) used to restore the grid to
14x14 afterward, so nothing was re-saved to the settings log this round.

--------------------------------------------------------------------------------
<next entry follows the same divider + dated-header pattern>

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.
